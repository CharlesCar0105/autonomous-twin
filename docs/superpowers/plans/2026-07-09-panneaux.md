# Feature Panneaux (M5) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal :** Détection + classification de panneaux (Stop/30/50/90/Aucun) et prise de décision dans le pilote — le +3 pts « Lecture de Panneaux » du barème.

**Architecture :** Sprites PNG générés par script (source unique simu + dataset) ; placement par sidecars JSON produits via dry-run PID ; compositing caméra pattern `Wall` ; MobileNetV2 gelé + tête 5 classes entraînée sur features précalculées (CPU) ; détecteur couleur → crop → classif → hystérésis → décision (limite persistante / STOP state machine) dans `pilot/main.py`.

**Tech Stack :** Python 3.12 (venv `.venv/Scripts/python.exe`), PyTorch 2.11 CPU, torchvision 0.26, PIL 12.2, pygame 2.6, numpy. Pas de nouvelle dépendance.

**Spec :** `docs/superpowers/specs/2026-07-09-panneaux-design.md` (approuvée, amendée §3.2/§3.4).

## Global Constraints

- Branche : `dev/panneaux-lorenzo`. Commits en français, style repo (`feat(scope): ...`).
- Tout le code/commentaires en **français**, snake_case, style des fichiers voisins.
- Python : TOUJOURS `.venv/Scripts/python.exe` (Windows), cwd = racine repo.
- Le pilote ne lit **JAMAIS** les `.signs.json` (anti-triche CDC) — caméra uniquement.
- Couleurs : rouge sprite `r ≥ 200` (STOP fill `(220,30,30)`, anneaux idem) ; seuil détecteur `r ≥ 190 & g < 90 & b < 90` ; brique mur = `(150,45,35)` / liseré `(180,70,55)` → jamais détectée.
- Ordre canonique des classes (alphabétique, partout) : `SIGN_CLASSES = ["30", "50", "90", "aucun", "stop"]`.
- Taille panneau monde : **36 px**. Crop classifieur : **96×96**. Caméra : 128×128, centrée 60 px devant la voiture (`sensors.py`).
- `track.pixels` n'est **jamais** modifié (pattern Wall).
- Le repo n'a pas de pytest : les vérifications = blocs `if __name__ == "__main__":` avec asserts + harness `scripts/test_*.py` avec verdicts PASS/FAIL imprimés (pattern `test_cnn.py`).
- Existant intouché : `test_cnn.py --with-signs` est un ajout opt-in ; `get_camera_view_from_track(..., signs=None)` par défaut = comportement actuel bit-identique.
- Le download des poids ImageNet MobileNetV2 (~14 Mo, 1 fois, cache `~/.cache/torch`) nécessite internet à la première exécution du training.

---

### Task 1 : Sprites — `scripts/gen_signs.py` → `assets/signs/*.png`

**Files:**
- Create: `scripts/gen_signs.py`
- Create (générés): `assets/signs/stop.png`, `assets/signs/30.png`, `assets/signs/50.png`, `assets/signs/90.png`

**Interfaces:**
- Produces: 4 PNG RGBA 256×256 (masters). Rouge vif `(220,30,30)`. Fond transparent hors panneau.

- [ ] **Step 1 : Écrire le script**

```python
"""
gen_signs.py -- Dessine les 4 sprites panneaux (masters 256x256 RGBA).

STOP  : octogone rouge vif + texte STOP blanc.
30/50/90 : disque blanc, anneau rouge vif, chiffres noirs (style FR).

Rouge choisi >= 200 pour rester discriminable du mur brique de Charles
(COL_BRICK = (150,45,35), lisere (180,70,55)) par le seuil detecteur r>=190.

Usage : python scripts/gen_signs.py   # ecrit dans assets/signs/
"""

import math
from pathlib import Path

from PIL import Image, ImageDraw, ImageFont

ROOT = Path(__file__).resolve().parent.parent
OUT_DIR = ROOT / "assets" / "signs"

SIZE = 256                     # master haute resolution (downscale a la pose)
RED = (220, 30, 30, 255)       # rouge vif panneau (r >= 200, cf spec)
WHITE = (255, 255, 255, 255)
BLACK = (10, 10, 10, 255)
RING_WIDTH = 30                # epaisseur anneau des limites de vitesse


def _font(size: int) -> ImageFont.FreeTypeFont:
    """Police bold si dispo (Windows), sinon fallback PIL."""
    for name in ("arialbd.ttf", "arial.ttf"):
        try:
            return ImageFont.truetype(name, size)
        except OSError:
            continue
    return ImageFont.load_default(size)


def _center_text(draw: ImageDraw.ImageDraw, text: str, font, fill) -> None:
    l, t, r, b = draw.textbbox((0, 0), text, font=font)
    draw.text(((SIZE - (r - l)) / 2 - l, (SIZE - (b - t)) / 2 - t),
              text, font=font, fill=fill)


def draw_stop() -> Image.Image:
    img = Image.new("RGBA", (SIZE, SIZE), (0, 0, 0, 0))
    d = ImageDraw.Draw(img)
    c, r = SIZE / 2, SIZE / 2 - 4
    pts = [(c + r * math.cos(math.radians(22.5 + 45 * i)),
            c + r * math.sin(math.radians(22.5 + 45 * i))) for i in range(8)]
    d.polygon(pts, fill=RED, outline=WHITE, width=10)
    _center_text(d, "STOP", _font(78), WHITE)
    return img


def draw_limit(number: str) -> Image.Image:
    img = Image.new("RGBA", (SIZE, SIZE), (0, 0, 0, 0))
    d = ImageDraw.Draw(img)
    d.ellipse([4, 4, SIZE - 4, SIZE - 4], fill=WHITE)
    d.ellipse([4, 4, SIZE - 4, SIZE - 4], outline=RED, width=RING_WIDTH)
    _center_text(d, number, _font(110), BLACK)
    return img


def main() -> None:
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    sprites = {"stop": draw_stop(), "30": draw_limit("30"),
               "50": draw_limit("50"), "90": draw_limit("90")}
    for name, img in sprites.items():
        path = OUT_DIR / f"{name}.png"
        img.save(path)
        print(f"[gen_signs] {path.name}  {img.size}  OK")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2 : Générer et vérifier programmatiquement**

Run : `.venv/Scripts/python.exe scripts/gen_signs.py`
Expected : 4 lignes `OK`.

Run (vérif couleurs/alpha) :
```
.venv/Scripts/python.exe -c "
import numpy as np; from PIL import Image
for n in ('stop','30','50','90'):
    a = np.array(Image.open(f'assets/signs/{n}.png'))
    assert a.shape == (256,256,4), n
    red = (a[...,0]>=200)&(a[...,1]<90)&(a[...,2]<90)&(a[...,3]>0)
    assert red.sum() > 3000, (n, red.sum())          # assez de rouge vif detectable
    assert (a[...,3]==0).mean() > 0.10, n            # coins transparents
print('sprites OK')"
```
Expected : `sprites OK`.

- [ ] **Step 3 : Vérification visuelle** — Read les 4 PNG (outil Read) et confirmer : octogone STOP lisible, chiffres 30/50/90 nets et distincts.

- [ ] **Step 4 : Commit**

```bash
git add scripts/gen_signs.py assets/signs/
git commit -m "feat(signs): sprites panneaux generes par script (stop/30/50/90)"
```

---

### Task 2 : `simulator/signs.py` — RoadSign + load_signs

**Files:**
- Create: `simulator/signs.py`

**Interfaces:**
- Consumes: `assets/signs/<kind>.png` (Task 1).
- Produces:
  - `RoadSign(x: float, y: float, kind: str, size: int = 36)` — attributs `.x .y .kind .size`
  - `RoadSign.paste_into_camera(crop: np.ndarray, x0: int, y0: int) -> None` — crop **(W,H,3) uint8 non transposé** (layout `track.pixels`), région monde commençant à `(x0, y0)` ; collage alpha in-place.
  - `RoadSign.draw(screen: pygame.Surface) -> None` — blit écran (Surface lazy).
  - `load_signs(circuit_name: str) -> list[RoadSign]` — lit `assets/tracks/<circuit>.signs.json`, `[]` si absent.
  - Format sidecar : `{"signs": [{"x": 123.0, "y": 456.0, "kind": "30"}, ...]}`

- [ ] **Step 1 : Écrire le module**

```python
"""
signs.py -- Panneaux routiers statiques (CDC : "images statiques (PNG) sur
les bords de la piste").

Pattern Wall (une source de verite, track.pixels jamais modifie), en plus
simple : un panneau n'a AUCUN role physique (pas de collision, pas de lidar
-- il est hors piste, au-dela de la bordure ou les rayons s'arretent deja).
Il n'existe que pour la camera (paste_into_camera) et l'ecran (draw).

Placement : sidecar assets/tracks/<circuit>.signs.json (genere par
scripts/place_signs.py). Le pilote ne lit JAMAIS ces fichiers (anti-triche) :
ils ne servent qu'au simulateur et aux harness d'eval.
"""

import json
from pathlib import Path

import numpy as np
import pygame
from PIL import Image

ROOT = Path(__file__).resolve().parent.parent
SIGNS_DIR = ROOT / "assets" / "signs"
TRACKS_DIR = ROOT / "assets" / "tracks"

SIGN_KINDS = ("stop", "30", "50", "90")
SIGN_SIZE_DEFAULT = 36   # diametre en px monde (crop camera = 128 px)

_SPRITE_CACHE: dict[tuple[str, int], np.ndarray] = {}


def _load_sprite(kind: str, size: int) -> np.ndarray:
    """Sprite (W, H, 4) uint8, axe 0 = x monde (layout track.pixels)."""
    key = (kind, size)
    if key not in _SPRITE_CACHE:
        img = Image.open(SIGNS_DIR / f"{kind}.png").convert("RGBA")
        img = img.resize((size, size), Image.LANCZOS)
        arr = np.array(img)                      # (H, W, 4), axe 0 = y
        _SPRITE_CACHE[key] = np.transpose(arr, (1, 0, 2)).copy()
    return _SPRITE_CACHE[key]


class RoadSign:
    """Panneau statique pose en (x, y) monde (centre du sprite)."""

    def __init__(self, x: float, y: float, kind: str,
                 size: int = SIGN_SIZE_DEFAULT) -> None:
        if kind not in SIGN_KINDS:
            raise ValueError(f"kind inconnu : {kind!r} (attendu {SIGN_KINDS})")
        self.x = float(x)
        self.y = float(y)
        self.kind = kind
        self.size = int(size)
        self._sprite = _load_sprite(kind, self.size)   # (W, H, 4)
        self._surf: pygame.Surface | None = None       # lazy (display requis)

    # --- Camera (vue pilote) ----------------------------------------------

    def paste_into_camera(self, crop: np.ndarray, x0: int, y0: int) -> None:
        """Colle le sprite (alpha) dans un crop (W, H, 3) NON transpose,
        region monde [x0, x0+W) x [y0, y0+H). Meme convention d'indexation
        que Wall.mask_for_region. In-place, no-op si hors region."""
        s = self.size
        left = int(round(self.x - s / 2.0))
        top = int(round(self.y - s / 2.0))
        w, h = crop.shape[0], crop.shape[1]

        ox1, oy1 = max(left, x0), max(top, y0)
        ox2, oy2 = min(left + s, x0 + w), min(top + s, y0 + h)
        if ox1 >= ox2 or oy1 >= oy2:
            return

        sub = self._sprite[ox1 - left:ox2 - left, oy1 - top:oy2 - top]
        alpha = sub[..., 3:4].astype(np.float32) / 255.0
        region = crop[ox1 - x0:ox2 - x0, oy1 - y0:oy2 - y0].astype(np.float32)
        blended = alpha * sub[..., :3].astype(np.float32) + (1.0 - alpha) * region
        crop[ox1 - x0:ox2 - x0, oy1 - y0:oy2 - y0] = blended.astype(np.uint8)

    # --- Ecran (rendu simulateur) ------------------------------------------

    def draw(self, screen: pygame.Surface) -> None:
        if self._surf is None:
            arr = np.transpose(self._sprite, (1, 0, 2))  # (H, W, 4)
            self._surf = pygame.image.frombytes(
                arr.tobytes(), (arr.shape[1], arr.shape[0]), "RGBA"
            ).convert_alpha()
        screen.blit(self._surf,
                    (int(self.x - self.size / 2), int(self.y - self.size / 2)))


def load_signs(circuit_name: str) -> list[RoadSign]:
    """Charge le sidecar du circuit. Fichier absent = pas de panneaux."""
    path = TRACKS_DIR / f"{circuit_name}.signs.json"
    if not path.exists():
        return []
    data = json.loads(path.read_text(encoding="utf-8"))
    return [RoadSign(s["x"], s["y"], s["kind"]) for s in data.get("signs", [])]


if __name__ == "__main__":
    # Self-check : collage alpha correct, coins transparents preserves.
    crop = np.full((128, 128, 3), 255, dtype=np.uint8)      # fond blanc
    sign = RoadSign(600.0 + 64, 400.0 + 64, "stop")          # centre du crop
    sign.paste_into_camera(crop, 600, 400)
    r, g, b = crop[..., 0].astype(int), crop[..., 1].astype(int), crop[..., 2].astype(int)
    red = (r >= 190) & (g < 90) & (b < 90)
    assert red.sum() > 200, f"rouge insuffisant : {red.sum()}"
    assert (crop[0, 0] == 255).all(), "coin du crop modifie hors sprite"
    corner = crop[64 - 18, 64 - 18]                          # coin du carre sprite (octogone -> transparent)
    assert (corner == 255).all(), f"alpha ignore : {corner}"
    before = crop.copy()
    RoadSign(0.0, 0.0, "30").paste_into_camera(crop, 600, 400)  # tres loin
    assert (crop == before).all(), "no-op hors region viole"
    assert load_signs("circuit_inexistant_xyz") == []
    print("simulator/signs.py self-check OK")
```

- [ ] **Step 2 : Lancer le self-check**

Run : `.venv/Scripts/python.exe -m simulator.signs`
Expected : `simulator/signs.py self-check OK`

- [ ] **Step 3 : Commit**

```bash
git add simulator/signs.py
git commit -m "feat(signs): RoadSign (paste camera alpha + draw) + load_signs sidecar"
```

---

### Task 3 : Câblage capteurs + simulateur — panneaux visibles

**Files:**
- Modify: `simulator/sensors.py:66-96` (`get_camera_view_from_track`)
- Modify: `simulator/main.py` (chargement sidecar, param caméra, rendu, `--no-signs`)

**Interfaces:**
- Consumes: `RoadSign`, `load_signs` (Task 2).
- Produces: `get_camera_view_from_track(track, car, wall=None, signs=None)` — signature étendue, défaut `None` = comportement actuel bit-identique. Ordre compositing : **panneaux d'abord, mur ensuite** (le mur occlut).

- [ ] **Step 1 : Étendre `get_camera_view_from_track`**

Dans `simulator/sensors.py`, remplacer la signature et le bloc compositing :

```python
def get_camera_view_from_track(track: "Track", car: "Car", wall=None,
                               signs=None) -> np.ndarray:
```

Docstring : ajouter à la suite du paragraphe `wall:` :

```python
        signs: Liste de RoadSign a compositer dans la vue (panneaux visibles
               par le pilote). None/[] = piste seule. Les panneaux sont
               peints AVANT le mur : un mur qui spawn devant un panneau
               l'occlut (obstacle au premier plan).
```

Et le corps (remplacer le bloc `crop = ... return`) :

```python
    crop = track.pixels[x:x + CAMERA_WIDTH, y:y + CAMERA_HEIGHT].copy()
    if signs:
        for sign in signs:
            sign.paste_into_camera(crop, x, y)
    if wall is not None:
        wmask = wall.mask_for_region(x, y, CAMERA_WIDTH, CAMERA_HEIGHT)
        crop[wmask] = wall_camera_color()
    return np.transpose(crop, (1, 0, 2))
```

- [ ] **Step 2 : Vérifier la non-régression bit-identique (signs=None)**

Run :
```
.venv/Scripts/python.exe -c "
import os; os.environ.setdefault('SDL_VIDEODRIVER','dummy'); os.environ.setdefault('SDL_AUDIODRIVER','dummy')
import pygame; pygame.init(); pygame.display.set_mode((1,1))
import numpy as np
from simulator.track import Track; from simulator.car import Car
from simulator import sensors
from simulator.signs import RoadSign
t = Track('gen_000', 1280, 720); c = Car(t.start_x, t.start_y, t.start_angle)
base = sensors.get_camera_view_from_track(t, c)
avec = sensors.get_camera_view_from_track(t, c, signs=[])
assert (base == avec).all(), 'signs=[] doit etre identique'
cx = c.x + 60*np.cos(c.angle); cy = c.y + 60*np.sin(c.angle)
s = RoadSign(cx, cy, 'stop')
vue = sensors.get_camera_view_from_track(t, c, signs=[s])
red = (vue[...,0].astype(int)>=190)&(vue[...,1].astype(int)<90)&(vue[...,2].astype(int)<90)
assert red.sum() > 200, f'panneau invisible dans la camera : {red.sum()}'
assert not (vue == base).all()
print('sensors signs OK')"
```
Expected : `sensors signs OK`

- [ ] **Step 3 : Câbler `simulator/main.py`**

3a. Imports (après `from simulator.timing import ...`) :
```python
from simulator.signs import load_signs
```

3b. Argument CLI (après `--start-pos`) :
```python
    parser.add_argument("--no-signs", action="store_true",
                        help="Ne charge pas les panneaux du circuit (sidecar .signs.json).")
```

3c. Init (à côté de `wall = None` / `lap_timer = ...`) :
```python
    # Panneaux statiques du circuit (sidecar JSON, absent = aucun).
    signs = [] if args.no_signs else load_signs(args.circuit)
    if signs:
        print(f"[Simulateur] {len(signs)} panneau(x) charges : "
              + ", ".join(s.kind for s in signs))
```

3d. Boucle ZMQ — remplacer les deux appels capteurs :
```python
            lidar = sensors.get_lidar(track, car, wall)
            camera = sensors.get_camera_view_from_track(track, car, wall, signs=signs)
```
(`get_lidar` inchangé : les panneaux n'arrêtent pas les rayons.)

3e. Rendu — juste après `draw_finish_line(screen, lap_timer)` :
```python
        for sign in signs:
            sign.draw(screen)
```

- [ ] **Step 4 : Vérif visuelle manuelle (fenêtré)**

Créer un sidecar temporaire de test :
```
.venv/Scripts/python.exe -c "
import json
from simulator.track import Track
import os; os.environ.setdefault('SDL_VIDEODRIVER','dummy'); os.environ.setdefault('SDL_AUDIODRIVER','dummy')
import pygame; pygame.init(); pygame.display.set_mode((1,1))
t = Track('circuit_02', 1280, 720)
json.dump({'signs':[{'x': t.start_x+80, 'y': t.start_y+60, 'kind':'stop'}]},
          open('assets/tracks/circuit_02.signs.json','w'), indent=2)
print('sidecar test ecrit')"
```
Puis lancer `.venv/Scripts/python.exe -m simulator.main` (fenêtré, circuit_02 par défaut) : le panneau STOP doit être visible à l'écran près du spawn, et dans l'aperçu caméra (touche C) quand la voiture le regarde. Fermer, puis **supprimer le sidecar de test** (`rm assets/tracks/circuit_02.signs.json` — il sera régénéré proprement en Task 4).

- [ ] **Step 5 : Commit**

```bash
git add simulator/sensors.py simulator/main.py
git commit -m "feat(signs): compositing camera + rendu ecran + chargement sidecar (--no-signs)"
```

---

### Task 4 : `scripts/place_signs.py` — sidecars par dry-run PID

**Files:**
- Create: `scripts/place_signs.py`
- Create (générés) : `assets/tracks/gen_*.signs.json` (30, gitignorés), `assets/tracks/circuit_01.signs.json`, `assets/tracks/circuit_02.signs.json` (committés)

**Interfaces:**
- Consumes: `pid_policy` (`pilot/control.py`), `Track`, `Car`, `physics`, `sensors` — même harness headless que `scripts/test_pid.py`.
- Produces: sidecars au format Task 2. CLI : `place_signs.py --tracks gen_000 ... | --pattern "gen_*" [--per-track 3] [--seed 42] [--seconds 40]`.

- [ ] **Step 1 : Écrire le script**

```python
"""
place_signs.py -- Genere les sidecars <circuit>.signs.json par dry-run PID.

Principe : le PID n'utilise QUE le lidar, sa trajectoire est donc identique
avec ou sans panneaux -> on le fait rouler headless, on echantillonne des
points le long de la trajectoire (espacement arclength >= 300 px : le crop
camera fait 128 px, jamais 2 panneaux dans la meme vue), et on pose chaque
panneau perpendiculairement, au premier offset (55/70/85 px) dont le segment
vers le point trajectoire croise une bordure noire -> panneau garanti HORS
piste (l'herbe et la route sont toutes deux blanches, seule la bordure les
separe).

Un seul chemin de code pour tous les circuits (gen_* et historiques).
Deterministe : PID + spawn fixes ; seul le type de panneau est tire au sort
(rng seede).

Usage :
    python scripts/place_signs.py --pattern "gen_*"                  # les 30
    python scripts/place_signs.py --tracks circuit_01 circuit_02
"""

import argparse
import json
import math
import os
import sys
from pathlib import Path

os.environ.setdefault("SDL_VIDEODRIVER", "dummy")
os.environ.setdefault("SDL_AUDIODRIVER", "dummy")

import numpy as np
import pygame

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))

from simulator.track import Track
from simulator.car import Car
from simulator import physics, sensors
from simulator.signs import SIGN_KINDS
from pilot.control import pid_policy

SPACING_PX = 300.0          # arclength mini entre 2 panneaux
OFFSETS_PX = (55.0, 70.0, 85.0)   # offsets lateraux candidats
SIGN_HALF = 18.0            # demi-taille panneau (36 px)


def _record_trajectory(track_name: str, seconds: float, fps: int = 60) -> list[dict]:
    """Roule le PID headless, retourne [{x, y, angle, arclen}] par frame."""
    pygame.init()
    pygame.display.set_mode((1, 1))
    track = Track(track_name, 1280, 720)
    car = Car(track.start_x, track.start_y, track.start_angle)
    dt = 1.0 / fps
    traj, arclen, px, py = [], 0.0, car.x, car.y
    for _ in range(int(seconds * fps)):
        lidar = sensors.get_lidar(track, car)
        steering, throttle, brake = pid_policy(lidar, physics.speed_kmh(car),
                                               speed_target=40.0)
        car.set_controls(steering, throttle, brake)
        physics.update(car, dt)
        arclen += math.hypot(car.x - px, car.y - py)
        px, py = car.x, car.y
        traj.append({"x": car.x, "y": car.y, "angle": car.angle,
                     "arclen": arclen})
    pygame.quit()
    return traj, track


def _crosses_border(track: Track, x1: float, y1: float, x2: float, y2: float) -> bool:
    """True si le segment [p1,p2] traverse au moins un pixel de bordure noire."""
    n = max(2, int(math.hypot(x2 - x1, y2 - y1)))
    black = 0
    for i in range(n + 1):
        t = i / n
        px_, py_ = int(x1 + t * (x2 - x1)), int(y1 + t * (y2 - y1))
        if 0 <= px_ < track.width and 0 <= py_ < track.height:
            r, g, b = track.pixels[px_, py_]
            if r < 80 and g < 80 and b < 80:
                black += 1
    return black >= 2


def _place_for_track(track_name: str, per_track: int, seconds: float,
                     rng: np.random.Generator) -> list[dict]:
    traj, track = _record_trajectory(track_name, seconds)
    if not traj:
        return []
    total = traj[-1]["arclen"]
    signs, used_arclen = [], []
    # Candidats : frames triees pour couvrir le tour, tirage rng sur l'ordre.
    order = rng.permutation(len(traj))
    for idx in order:
        if len(signs) >= per_track:
            break
        f = traj[idx]
        if f["arclen"] < 150.0:          # pas de panneau sur la ligne de depart
            continue
        if any(abs(f["arclen"] - u) < SPACING_PX for u in used_arclen):
            continue
        perp = (-math.sin(f["angle"]), math.cos(f["angle"]))
        for off in OFFSETS_PX:
            sx = f["x"] + perp[0] * off
            sy = f["y"] + perp[1] * off
            in_frame = (SIGN_HALF < sx < track.width - SIGN_HALF
                        and SIGN_HALF < sy < track.height - SIGN_HALF)
            if in_frame and _crosses_border(track, f["x"], f["y"], sx, sy):
                kind = str(rng.choice(SIGN_KINDS))
                signs.append({"x": round(sx, 1), "y": round(sy, 1),
                              "kind": kind, "arclen": round(f["arclen"], 1)})
                used_arclen.append(f["arclen"])
                break
    for s in signs:
        s.pop("arclen")
    return signs


def main() -> None:
    parser = argparse.ArgumentParser(description="Genere les sidecars panneaux")
    parser.add_argument("--tracks", nargs="*", default=None)
    parser.add_argument("--pattern", default=None, help='ex: "gen_*"')
    parser.add_argument("--per-track", type=int, default=3)
    parser.add_argument("--seconds", type=float, default=40.0)
    parser.add_argument("--seed", type=int, default=42)
    args = parser.parse_args()

    tracks_dir = ROOT / "assets" / "tracks"
    if args.tracks:
        names = args.tracks
    elif args.pattern:
        names = sorted(p.stem for p in tracks_dir.glob(f"{args.pattern}.png"))
    else:
        parser.error("--tracks ou --pattern requis")

    for i, name in enumerate(names):
        rng = np.random.default_rng(args.seed + i)
        signs = _place_for_track(name, args.per_track, args.seconds, rng)
        out = tracks_dir / f"{name}.signs.json"
        out.write_text(json.dumps({"signs": signs}, indent=2), encoding="utf-8")
        kinds = ", ".join(s["kind"] for s in signs) or "AUCUN"
        print(f"  [{i+1:2d}/{len(names)}] {out.name:28s} {len(signs)} panneaux ({kinds})")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2 : Générer et vérifier**

Run : `.venv/Scripts/python.exe scripts/place_signs.py --pattern "gen_*"`
Expected : 30 lignes, 2-3 panneaux/circuit (≥ 2 sur la quasi-totalité ; 0 = investigation).

Run : `.venv/Scripts/python.exe scripts/place_signs.py --tracks circuit_01 circuit_02`
Expected : 2 sidecars créés.

Vérif hors-piste (échantillon) :
```
.venv/Scripts/python.exe -c "
import os, json; os.environ.setdefault('SDL_VIDEODRIVER','dummy'); os.environ.setdefault('SDL_AUDIODRIVER','dummy')
import pygame; pygame.init(); pygame.display.set_mode((1,1))
from simulator.track import Track
for name in ('gen_000','gen_014','circuit_01'):
    t = Track(name, 1280, 720)
    d = json.load(open(f'assets/tracks/{name}.signs.json'))
    print(name, len(d['signs']), 'panneaux')
    assert len(d['signs']) >= 1, name
print('sidecars OK')"
```
Expected : `sidecars OK`.

- [ ] **Step 3 : Vérif déterminisme**

Relancer `place_signs.py --tracks gen_000` deux fois, comparer :
Run : `.venv/Scripts/python.exe scripts/place_signs.py --tracks gen_000 && cp assets/tracks/gen_000.signs.json /tmp/a.json && .venv/Scripts/python.exe scripts/place_signs.py --tracks gen_000 && diff /tmp/a.json assets/tracks/gen_000.signs.json && echo DETERMINISTE`
Expected : `DETERMINISTE`.

- [ ] **Step 4 : Commit** (gen_* gitignorés par le pattern `gen_*.json` existant — vérifier avec `git status` que seuls circuit_01/02 apparaissent)

```bash
git add scripts/place_signs.py assets/tracks/circuit_01.signs.json assets/tracks/circuit_02.signs.json
git commit -m "feat(signs): place_signs.py -- sidecars par dry-run PID (tous circuits)"
```

---

### Task 5 : `scripts/gen_signs_dataset.py` — dataset synthétique

**Files:**
- Create: `scripts/gen_signs_dataset.py`
- Modify: `.gitignore` (ajouter `data/signs/`)
- Create (générés, gitignorés) : `data/signs/{train,val}/{30,50,90,aucun,stop}/*.png`

**Interfaces:**
- Consumes: fonds = clé `camera` des `.npz` de `data/train/*/` (train) et `data/val/*/` (val) ; sprites Task 1 ; constantes couleur mur `simulator.wall.COL_BRICK, COL_MORTAR, COL_BRICK_HI`.
- Produces: PNG 96×96 classés par dossier, ~1500/classe train, ~300/classe val. Seed 42.

- [ ] **Step 1 : Écrire le script**

```python
"""
gen_signs_dataset.py -- Dataset synthetique classif panneaux (D7).

Fonds : crops camera 128x128 REELS tires des .npz du dataset conduite
(train <- data/train = gen_000..024, val <- data/val = gen_025..029 ; D10 :
les fonds du val viennent de circuits jamais vus par le train).

Positifs : sprite colle sur fond avec augmentations (echelle, rotation +/-20,
luminosite, bruit, flou, troncature partielle 20%) puis crop 96x96 centre
sur le panneau (simule la sortie du detecteur bbox+marge).

"aucun" : moitie fonds nus, moitie crops de MUR brique (memes couleurs que
simulator/wall.py) -> le classifieur apprend a rejeter la brique.

Usage : python scripts/gen_signs_dataset.py   # ~9k images, seed 42
"""

import argparse
import sys
from pathlib import Path

import numpy as np
from PIL import Image, ImageDraw, ImageEnhance, ImageFilter

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))

from simulator.wall import COL_BRICK, COL_MORTAR, COL_BRICK_HI  # source unique

SIGN_KINDS = ("stop", "30", "50", "90")
CLASSES = ("30", "50", "90", "aucun", "stop")
OUT_SIZE = 96
MARGIN = 1.4                     # bbox + 20% de chaque cote (facteur 1.4)


def _load_backgrounds(split_dir: Path, rng: np.random.Generator, k: int) -> list[np.ndarray]:
    files = sorted(split_dir.glob("*/*.npz"))
    assert files, f"aucun npz dans {split_dir} -- lancer record_dataset.py d'abord"
    picks = rng.choice(len(files), size=k, replace=True)
    return [np.load(files[i])["camera"] for i in picks]


def _augment_sprite(kind: str, rng: np.random.Generator) -> Image.Image:
    img = Image.open(ROOT / "assets" / "signs" / f"{kind}.png").convert("RGBA")
    size = int(rng.uniform(25, 50))                       # 36 px nominal en camera
    img = img.resize((size, size), Image.LANCZOS)
    img = img.rotate(float(rng.uniform(-20, 20)), expand=True,
                     resample=Image.BICUBIC)
    return img


def _paste_and_crop(bg: np.ndarray, sprite: Image.Image,
                    rng: np.random.Generator, truncate: bool) -> Image.Image:
    canvas = Image.fromarray(bg).convert("RGBA")
    sw, sh = sprite.size
    if truncate:   # panneau partiellement hors champ (bord du crop camera)
        cx = int(rng.choice([rng.uniform(-sw * 0.3, sw * 0.2),
                             rng.uniform(128 - sw * 0.2, 128 - sw * 0.7 + sw)]))
        cy = int(rng.uniform(0, 128 - sh))
    else:
        cx = int(rng.uniform(0, max(1, 128 - sw)))
        cy = int(rng.uniform(0, max(1, 128 - sh)))
    canvas.alpha_composite(sprite, (cx, cy))
    canvas = canvas.convert("RGB")

    # Crop carre centre sur le panneau, bbox * MARGIN, clamp aux bords.
    ccx, ccy = cx + sw / 2, cy + sh / 2
    half = max(sw, sh) * MARGIN / 2
    x1, y1 = int(max(0, ccx - half)), int(max(0, ccy - half))
    x2, y2 = int(min(128, ccx + half)), int(min(128, ccy + half))
    crop = canvas.crop((x1, y1, x2, y2)).resize((OUT_SIZE, OUT_SIZE), Image.BILINEAR)

    # Photometrie : luminosite, flou leger, bruit gaussien.
    crop = ImageEnhance.Brightness(crop).enhance(float(rng.uniform(0.7, 1.3)))
    if rng.random() < 0.5:
        crop = crop.filter(ImageFilter.GaussianBlur(float(rng.uniform(0.3, 1.2))))
    arr = np.asarray(crop).astype(np.int16)
    arr += rng.normal(0.0, rng.uniform(0, 8), arr.shape).astype(np.int16)
    return Image.fromarray(np.clip(arr, 0, 255).astype(np.uint8))


def _wall_patch(bg: np.ndarray, rng: np.random.Generator) -> Image.Image:
    """Faux positif potentiel : morceau de mur brique sur fond reel."""
    canvas = Image.fromarray(bg).convert("RGB")
    d = ImageDraw.Draw(canvas)
    w = int(rng.uniform(40, 110)); h = int(rng.uniform(16, 34))
    x = int(rng.uniform(0, 128 - w)); y = int(rng.uniform(0, 128 - h))
    d.rectangle([x, y, x + w, y + h], fill=COL_BRICK, outline=COL_BRICK_HI, width=2)
    for i in range(1, 5):
        yy = y + i * h // 5
        d.line([x, yy, x + w, yy], fill=COL_MORTAR, width=2)
    half = int(max(w, h) * MARGIN / 2); ccx, ccy = x + w // 2, y + h // 2
    x1, y1 = max(0, ccx - half), max(0, ccy - half)
    x2, y2 = min(128, ccx + half), min(128, ccy + half)
    return canvas.crop((x1, y1, x2, y2)).resize((OUT_SIZE, OUT_SIZE), Image.BILINEAR)


def _empty_patch(bg: np.ndarray, rng: np.random.Generator) -> Image.Image:
    s = int(rng.uniform(30, 70))
    x = int(rng.uniform(0, 128 - s)); y = int(rng.uniform(0, 128 - s))
    return Image.fromarray(bg).convert("RGB").crop((x, y, x + s, y + s)).resize(
        (OUT_SIZE, OUT_SIZE), Image.BILINEAR)


def generate_split(split: str, n_per_class: int, seed: int) -> None:
    rng = np.random.default_rng(seed)
    bg_dir = ROOT / "data" / split
    out_root = ROOT / "data" / "signs" / split
    backgrounds = _load_backgrounds(bg_dir, rng, k=n_per_class * len(CLASSES))
    bg_iter = iter(backgrounds)

    for cls in CLASSES:
        out = out_root / cls
        out.mkdir(parents=True, exist_ok=True)
        for i in range(n_per_class):
            bg = next(bg_iter)
            if cls == "aucun":
                img = _wall_patch(bg, rng) if i % 2 == 0 else _empty_patch(bg, rng)
            else:
                sprite = _augment_sprite(cls, rng)
                img = _paste_and_crop(bg, sprite, rng, truncate=(rng.random() < 0.2))
            img.save(out / f"{i:05d}.png")
        print(f"  [{split}] {cls:6s} : {n_per_class} images")


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--train-per-class", type=int, default=1500)
    parser.add_argument("--val-per-class", type=int, default=300)
    parser.add_argument("--seed", type=int, default=42)
    args = parser.parse_args()
    generate_split("train", args.train_per_class, args.seed)
    generate_split("val", args.val_per_class, args.seed + 1)
    print("[gen_signs_dataset] termine.")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2 : `.gitignore`** — ajouter la ligne `data/signs/` (le bloc data/ existant peut déjà couvrir — vérifier avec `git check-ignore data/signs/train/stop/00000.png` après génération ; si déjà ignoré, pas de modif).

- [ ] **Step 3 : Générer et vérifier**

Run : `.venv/Scripts/python.exe scripts/gen_signs_dataset.py`
Expected : 10 lignes (5 classes × 2 splits), total 7500 train + 1500 val.

Vérif :
```
.venv/Scripts/python.exe -c "
from pathlib import Path
import numpy as np
from PIL import Image
root = Path('data/signs')
for split, n in (('train', 1500), ('val', 300)):
    for cls in ('30','50','90','aucun','stop'):
        files = list((root/split/cls).glob('*.png'))
        assert len(files) == n, (split, cls, len(files))
a = np.array(Image.open(next((root/'train'/'stop').glob('*.png'))))
assert a.shape == (96, 96, 3)
print('dataset OK')"
```
Expected : `dataset OK`.

- [ ] **Step 4 : Vérif visuelle** — Read 3-4 échantillons (un par classe, dont un `aucun` mur) et confirmer que les panneaux sont reconnaissables et les crops sensés.

- [ ] **Step 5 : Commit** (script seul, data gitignoré)

```bash
git add scripts/gen_signs_dataset.py .gitignore
git commit -m "feat(signs): generateur dataset synthetique (fonds reels + augmentations + anti-mur)"
```

---

### Task 6 : `pilot/signs_arch.py` + `scripts/train_signs.py` — entraînement

**Files:**
- Create: `pilot/signs_arch.py`
- Create: `scripts/train_signs.py`
- Create (générés) : `models/signs_cls.pth`, `models/signs_cls_history.json`

**Interfaces:**
- Consumes: `data/signs/{train,val}` (Task 5).
- Produces:
  - `pilot/signs_arch.py` : `SIGN_CLASSES = ["30", "50", "90", "aucun", "stop"]` (ordre canonique), `build_signs_net(pretrained: bool = True) -> torch.nn.Module` (MobileNetV2, features gelées, classifier = `nn.Sequential(nn.Dropout(0.2), nn.Linear(1280, 5))`), `IMAGENET_MEAN`, `IMAGENET_STD` (tuples de 3 floats).
  - `models/signs_cls.pth` : state_dict complet du réseau (chargeable avec `build_signs_net(pretrained=False)`).

- [ ] **Step 1 : Écrire `pilot/signs_arch.py`**

```python
"""
signs_arch.py -- Architecture du classifieur de panneaux (D5).

MobileNetV2 pretrained ImageNet, features GELEES, tete de classification
remplacee (Dropout + Linear 1280 -> 5). Input attendu : (B, 3, 96, 96)
normalise ImageNet.

Partage entre scripts/train_signs.py (training) et pilot/signs.py (inference),
comme cnn_drive_arch.py pour le CNN conduite.
"""

import torch.nn as nn
from torchvision import models

# Ordre canonique = tri alphabetique des dossiers dataset. NE PAS reordonner.
SIGN_CLASSES = ["30", "50", "90", "aucun", "stop"]

IMAGENET_MEAN = (0.485, 0.456, 0.406)
IMAGENET_STD = (0.229, 0.224, 0.225)


def build_signs_net(pretrained: bool = True) -> nn.Module:
    """Construit le reseau. pretrained=True telecharge les poids ImageNet
    (1ere fois seulement, cache torch) ; False pour recharger un .pth."""
    weights = models.MobileNet_V2_Weights.IMAGENET1K_V1 if pretrained else None
    net = models.mobilenet_v2(weights=weights)
    for p in net.features.parameters():
        p.requires_grad = False
    net.classifier = nn.Sequential(
        nn.Dropout(0.2),
        nn.Linear(net.last_channel, len(SIGN_CLASSES)),
    )
    return net
```

- [ ] **Step 2 : Écrire `scripts/train_signs.py`**

```python
"""
train_signs.py -- Entrainement de la tete du classifieur panneaux (CPU-ok).

Backbone gele (D5) -> on precalcule les features MobileNetV2 (1280-d) UNE
FOIS pour toutes les images (~2-3 min CPU), puis on entraine uniquement la
tete Linear sur ce cache (quelques secondes par epoch). Leçon audit Q8 :
l'historique complet (loss/acc train+val par epoch) est persiste en JSON a
cote du modele.

Sorties :
    models/signs_cls.pth           state_dict COMPLET (features ImageNet + tete)
    models/signs_cls_history.json  historique + matrice de confusion val

Usage : python scripts/train_signs.py [--epochs 15] [--lr 1e-3]
"""

import argparse
import json
import sys
import time
from pathlib import Path

import numpy as np
import torch
import torch.nn as nn
import torch.nn.functional as F
from PIL import Image

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))

from pilot.signs_arch import (SIGN_CLASSES, IMAGENET_MEAN, IMAGENET_STD,
                              build_signs_net)

DATA = ROOT / "data" / "signs"
MODELS = ROOT / "models"


def _load_split_tensors(split: str) -> tuple[torch.Tensor, torch.Tensor]:
    """Charge toutes les images d'un split -> (X (N,3,96,96) normalise, y (N,))."""
    xs, ys = [], []
    for ci, cls in enumerate(SIGN_CLASSES):
        for f in sorted((DATA / split / cls).glob("*.png")):
            xs.append(np.asarray(Image.open(f), dtype=np.float32) / 255.0)
            ys.append(ci)
    x = torch.from_numpy(np.stack(xs)).permute(0, 3, 1, 2)
    mean = torch.tensor(IMAGENET_MEAN).view(1, 3, 1, 1)
    std = torch.tensor(IMAGENET_STD).view(1, 3, 1, 1)
    return (x - mean) / std, torch.tensor(ys, dtype=torch.long)


@torch.no_grad()
def _extract_features(net: nn.Module, x: torch.Tensor, batch: int = 64) -> torch.Tensor:
    """Features 1280-d (avant classifier), backbone en eval."""
    net.eval()
    outs = []
    for i in range(0, len(x), batch):
        f = net.features(x[i:i + batch])
        outs.append(F.adaptive_avg_pool2d(f, 1).flatten(1))
        if i % (batch * 20) == 0:
            print(f"    features {i}/{len(x)}")
    return torch.cat(outs)


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--epochs", type=int, default=15)
    parser.add_argument("--lr", type=float, default=1e-3)
    parser.add_argument("--batch", type=int, default=256)
    parser.add_argument("--seed", type=int, default=0)
    args = parser.parse_args()
    torch.manual_seed(args.seed)

    print("[train_signs] chargement images...")
    x_train, y_train = _load_split_tensors("train")
    x_val, y_val = _load_split_tensors("val")
    print(f"  train {tuple(x_train.shape)}  val {tuple(x_val.shape)}")

    net = build_signs_net(pretrained=True)

    print("[train_signs] extraction features (backbone gele, 1 seule fois)...")
    t0 = time.time()
    f_train = _extract_features(net, x_train)
    f_val = _extract_features(net, x_val)
    print(f"  features en {time.time()-t0:.0f}s : train {tuple(f_train.shape)}")

    head = nn.Sequential(nn.Dropout(0.2), nn.Linear(f_train.shape[1], len(SIGN_CLASSES)))
    opt = torch.optim.Adam(head.parameters(), lr=args.lr)
    history, best_acc, best_state = [], 0.0, None

    for epoch in range(1, args.epochs + 1):
        head.train()
        perm = torch.randperm(len(f_train))
        tot_loss = 0.0
        for i in range(0, len(perm), args.batch):
            idx = perm[i:i + args.batch]
            opt.zero_grad()
            loss = F.cross_entropy(head(f_train[idx]), y_train[idx])
            loss.backward()
            opt.step()
            tot_loss += loss.item() * len(idx)
        head.eval()
        with torch.no_grad():
            logits = head(f_val)
            val_loss = F.cross_entropy(logits, y_val).item()
            val_acc = (logits.argmax(1) == y_val).float().mean().item()
            train_acc = (head(f_train).argmax(1) == y_train).float().mean().item()
        history.append({"epoch": epoch, "train_loss": tot_loss / len(f_train),
                        "train_acc": train_acc, "val_loss": val_loss,
                        "val_acc": val_acc})
        star = ""
        if val_acc > best_acc:
            best_acc, best_state = val_acc, {k: v.clone() for k, v in head.state_dict().items()}
            star = "  *best"
        print(f"  epoch {epoch:2d}  train_acc {train_acc:.4f}  val_acc {val_acc:.4f}{star}")

    head.load_state_dict(best_state)

    # Matrice de confusion val (listes int, ordre SIGN_CLASSES).
    with torch.no_grad():
        pred = head(f_val).argmax(1)
    conf = [[int(((y_val == i) & (pred == j)).sum()) for j in range(len(SIGN_CLASSES))]
            for i in range(len(SIGN_CLASSES))]

    # Injecte la tete dans le reseau complet et sauve le state_dict entier.
    net.classifier.load_state_dict(best_state)
    MODELS.mkdir(exist_ok=True)
    torch.save(net.state_dict(), MODELS / "signs_cls.pth")
    (MODELS / "signs_cls_history.json").write_text(json.dumps({
        "best_val_acc": best_acc, "classes": SIGN_CLASSES,
        "confusion_val": conf, "history": history,
        "args": vars(args),
    }, indent=2), encoding="utf-8")

    print(f"[train_signs] best val_acc = {best_acc:.4f}")
    print(f"  confusion (lignes=verite, colonnes=pred, ordre {SIGN_CLASSES}) :")
    for cls, row in zip(SIGN_CLASSES, conf):
        print(f"    {cls:6s} {row}")
    print(f"  -> {MODELS/'signs_cls.pth'}")
    assert best_acc > 0.95, f"cible >95% non atteinte : {best_acc:.4f} (cf spec: fallback GTSRB si <90%)"


if __name__ == "__main__":
    main()
```

- [ ] **Step 3 : Entraîner**

Run : `.venv/Scripts/python.exe scripts/train_signs.py` (~5-10 min CPU, download poids ImageNet à la 1ère exécution)
Expected : `best val_acc >= 0.95`, assert final passe, matrice de confusion sans confusion 30↔90 majeure.

- [ ] **Step 4 : Vérifier le rechargement offline du .pth**

Run :
```
.venv/Scripts/python.exe -c "
import torch
from pilot.signs_arch import build_signs_net
net = build_signs_net(pretrained=False)
net.load_state_dict(torch.load('models/signs_cls.pth', map_location='cpu'))
net.eval()
print('reload OK, params:', sum(p.numel() for p in net.parameters()))"
```
Expected : `reload OK` (~3.5M params).

- [ ] **Step 5 : Commit**

```bash
git add pilot/signs_arch.py scripts/train_signs.py models/signs_cls.pth models/signs_cls_history.json
git commit -m "feat(signs): MobileNetV2 gele + tete 5 classes, training features precalculees (val acc >95%)"
```

---

### Task 7 : `pilot/signs.py` — détection, classification, tracker

**Files:**
- Modify (réécrire, stub 13 lignes): `pilot/signs.py`

**Interfaces:**
- Consumes: `build_signs_net`, `SIGN_CLASSES`, `IMAGENET_MEAN/STD` (Task 6) ; `models/signs_cls.pth`.
- Produces:
  - `detect_sign_bbox(camera: np.ndarray) -> tuple[int,int,int,int] | None` — camera (H,W,3) uint8 ; bbox (x1,y1,x2,y2) élargie 20 %, ou None.
  - `classify_crop(camera, bbox, weights_path=None) -> tuple[str, float]` — (kind ∈ SIGN_CLASSES, confiance softmax).
  - `SignTracker()` : `.update(camera, speed_kmh: float, now: float) -> None` ; propriétés `.speed_limit: float | None`, `.stop_active: bool`, `.last_detection: str | None` (pour log/dashboard).

- [ ] **Step 1 : Écrire le module**

```python
"""
signs.py -- Detection et classification des panneaux routiers (M5).

Pipeline camera-only (anti-triche CDC : le pilote ne lit jamais les
sidecars .signs.json, il n'a que ses capteurs) :

    1. detect_sign_bbox : masque couleur rouge vif -> bbox.
       Seuil r>=190 & g<90 & b<90 : le rouge sprite est (220,30,30), le mur
       brique (150,45,35) et son lisere (180,70,55) restent SOUS le seuil.
       # ponytail: bbox globale des pixels rouges (pas de composantes
       # connexes) -- l'espacement >=300px des panneaux garantit <=1 panneau
       # par vue ; passer a du connected-components si ca change.
    2. classify_crop : crop bbox -> preprocess_batch (224) -> MobileNetV2
       (models/signs_cls.pth, tete MLP — cf signs_arch).
    3. SignTracker : hysteresis 3 frames (conf >= 0.8) + cooldown 4 s.
       Limites 30/50/90 -> speed_limit persistante (regle routiere reelle).
       STOP -> BRAKING -> STOPPED (v < 2 km/h, maintien 2 s) -> RESUME.

Metrique cible : accuracy > 95% (atteinte au training, cf
models/signs_cls_history.json). Decision integree dans pilot/main.py.
"""

from pathlib import Path
from typing import Optional

import numpy as np

from pilot.signs_arch import SIGN_CLASSES, preprocess_batch

# --- Detection (couleur) ---------------------------------------------------

RED_MIN_R = 190
RED_MAX_GB = 90
MIN_RED_PIXELS = 60          # aire mini du blob rouge (px)
BBOX_MARGIN = 0.2            # elargissement bbox de 20 %
ASPECT_MIN, ASPECT_MAX = 0.5, 2.0

# --- Tracker ----------------------------------------------------------------

CONF_THRESHOLD = 0.8
HYSTERESIS_FRAMES = 3        # frames consecutives meme classe pour valider
COOLDOWN_S = 4.0             # apres application : ignore les detections
STOP_HOLD_S = 2.0            # arret complet maintenu (CDC "arret si Stop")
STOP_SPEED_KMH = 2.0         # seuil "voiture arretee"

_model = None


def _load_model(weights_path: Optional[str] = None):
    global _model
    if _model is not None:
        return
    import torch
    from pilot.signs_arch import build_signs_net
    if weights_path is None:
        weights_path = str(Path(__file__).resolve().parent.parent
                           / "models" / "signs_cls.pth")
    _model = build_signs_net(pretrained=False)
    _model.load_state_dict(torch.load(weights_path, map_location="cpu"))
    _model.eval()


def detect_sign_bbox(camera: np.ndarray):
    """Bbox (x1, y1, x2, y2) du blob rouge vif, ou None. camera (H, W, 3)."""
    r = camera[..., 0].astype(np.int16)
    g = camera[..., 1].astype(np.int16)
    b = camera[..., 2].astype(np.int16)
    red = (r >= RED_MIN_R) & (g < RED_MAX_GB) & (b < RED_MAX_GB)
    n = int(red.sum())
    if n < MIN_RED_PIXELS:
        return None
    ys, xs = np.nonzero(red)
    x1, x2 = int(xs.min()), int(xs.max()) + 1
    y1, y2 = int(ys.min()), int(ys.max()) + 1
    w, h = x2 - x1, y2 - y1
    if not (ASPECT_MIN <= w / max(h, 1) <= ASPECT_MAX):
        return None
    if n / max(w * h, 1) < 0.10:      # blob trop diffus = pas un panneau
        return None
    mx, my = int(w * BBOX_MARGIN), int(h * BBOX_MARGIN)
    H, W = camera.shape[:2]
    return (max(0, x1 - mx), max(0, y1 - my),
            min(W, x2 + mx), min(H, y2 + my))


def classify_crop(camera: np.ndarray, bbox, weights_path: Optional[str] = None):
    """Classifie le crop bbox. Retourne (classe, confiance softmax)."""
    import torch
    import torch.nn.functional as F
    _load_model(weights_path)
    x1, y1, x2, y2 = bbox
    crop = camera[y1:y2, x1:x2].astype(np.float32) / 255.0
    t = torch.from_numpy(crop).permute(2, 0, 1).unsqueeze(0)
    # preprocess_batch = pipeline canonique (normalise ImageNet + upscale
    # FEAT_INPUT_SIZE=224) partage avec le training — ne JAMAIS re-inliner.
    with torch.no_grad():
        probs = F.softmax(_model(preprocess_batch(t)), dim=1).squeeze(0)
    idx = int(probs.argmax())
    return SIGN_CLASSES[idx], float(probs[idx])


class SignTracker:
    """Hysteresis + cooldown + decision (limite persistante, machine STOP)."""

    def __init__(self, weights_path: Optional[str] = None) -> None:
        self._weights = weights_path
        self.speed_limit: Optional[float] = None
        self.last_detection: Optional[str] = None
        self._pending_kind: Optional[str] = None
        self._pending_count = 0
        self._cooldown_until = 0.0
        self._stop_state = "NONE"          # NONE | BRAKING | STOPPED
        self._stop_until = 0.0

    @property
    def stop_active(self) -> bool:
        return self._stop_state in ("BRAKING", "STOPPED")

    def _apply(self, kind: str, now: float) -> None:
        if kind in ("30", "50", "90"):
            self.speed_limit = float(kind)
        elif kind == "stop":
            self._stop_state = "BRAKING"
        self._cooldown_until = now + COOLDOWN_S
        self._pending_kind, self._pending_count = None, 0

    def update(self, camera: np.ndarray, speed_kmh: float, now: float) -> None:
        # Machine STOP prioritaire (independante des nouvelles detections).
        if self._stop_state == "BRAKING" and speed_kmh < STOP_SPEED_KMH:
            self._stop_state = "STOPPED"
            self._stop_until = now + STOP_HOLD_S
        elif self._stop_state == "STOPPED" and now >= self._stop_until:
            self._stop_state = "NONE"
            self._cooldown_until = now + COOLDOWN_S

        self.last_detection = None
        if now < self._cooldown_until or self.stop_active:
            return

        bbox = detect_sign_bbox(camera)
        if bbox is None:
            self._pending_kind, self._pending_count = None, 0
            return
        kind, conf = classify_crop(camera, bbox, self._weights)
        self.last_detection = f"{kind}({conf:.2f})"
        if kind == "aucun" or conf < CONF_THRESHOLD:
            self._pending_kind, self._pending_count = None, 0
            return
        if kind == self._pending_kind:
            self._pending_count += 1
        else:
            self._pending_kind, self._pending_count = kind, 1
        if self._pending_count >= HYSTERESIS_FRAMES:
            self._apply(kind, now)


if __name__ == "__main__":
    # Self-check sans modele : detecteur seul (le classif exige le .pth).
    cam = np.full((128, 128, 3), 255, dtype=np.uint8)
    assert detect_sign_bbox(cam) is None, "blanc pur ne doit rien detecter"
    cam[40:70, 50:80] = (220, 30, 30)                      # pseudo panneau
    bbox = detect_sign_bbox(cam)
    assert bbox is not None and bbox[0] <= 50 and bbox[2] >= 80, bbox
    cam2 = np.full((128, 128, 3), 255, dtype=np.uint8)
    cam2[40:70, 30:110] = (150, 45, 35)                    # mur brique
    cam2[40:42, 30:110] = (180, 70, 55)                    # lisere
    assert detect_sign_bbox(cam2) is None, "la brique ne doit PAS declencher"
    # Tracker : hysteresis + application limite (classif mockee).
    tr = SignTracker()
    tr._apply("30", now=100.0)
    assert tr.speed_limit == 30.0 and tr._cooldown_until == 104.0
    tr._apply("stop", now=200.0)
    assert tr.stop_active
    tr.update(cam, speed_kmh=1.0, now=201.0)               # v<2 -> STOPPED
    assert tr._stop_state == "STOPPED"
    tr.update(cam, speed_kmh=0.0, now=203.5)               # 2s ecoulees -> NONE
    assert tr._stop_state == "NONE" and not tr.stop_active
    print("pilot/signs.py self-check OK")
```

- [ ] **Step 2 : Lancer le self-check**

Run : `.venv/Scripts/python.exe -m pilot.signs`
Expected : `pilot/signs.py self-check OK`

- [ ] **Step 3 : Test classif bout-en-bout sur une vraie image dataset**

Run :
```
.venv/Scripts/python.exe -c "
import numpy as np
from PIL import Image
from pathlib import Path
from pilot.signs import detect_sign_bbox, classify_crop
ok = 0; tot = 0
for cls in ('stop','30','50','90'):
    for f in sorted(Path(f'data/signs/val/{cls}').glob('*.png'))[:20]:
        cam = np.array(Image.open(f))
        bbox = detect_sign_bbox(cam)
        if bbox is None: tot += 1; continue
        kind, conf = classify_crop(cam, bbox)
        tot += 1; ok += (kind == cls)
print(f'{ok}/{tot} crops val correctement re-detectes+classifies')
assert ok / tot > 0.85, 'pipeline detection+classif trop faible'"
```
Expected : ratio > 0.85 (les crops val sont déjà zoomés : la détection y est plus dure qu'en conditions réelles ; le vrai test est Task 9).

- [ ] **Step 4 : Commit**

```bash
git add pilot/signs.py
git commit -m "feat(signs): detection couleur + classif MobileNetV2 + SignTracker (hysteresis/cooldown/machine STOP)"
```

---

### Task 8 : Câblage `pilot/main.py` — governor + STOP

**Files:**
- Modify: `pilot/main.py`

**Interfaces:**
- Consumes: `SignTracker` (Task 7).
- Produces: boucle pilote avec panneaux actifs par défaut, flag `--no-signs`, log enrichi.

- [ ] **Step 1 : Câbler**

1a. Import (après `from pilot.perception import compute_mask`) :
```python
from pilot.signs import SignTracker
```

1b. Argument CLI (après `--no-emergency`) :
```python
    parser.add_argument("--no-signs", action="store_true",
                        help="Desactive la lecture de panneaux (detection+classif+decision).")
```

1c. Init (avant la boucle, après `client = PilotClient(...)`) :
```python
    tracker = None if args.no_signs else SignTracker()
```

1d. Dans la boucle, **après** le bloc politique (`commands = ...`), **avant** le bloc emergency :
```python
            # Panneaux : detection camera-only -> limite de vitesse / STOP.
            # Governor post-policy : un seul mecanisme, valable PID et CNN.
            if tracker is not None:
                tracker.update(sensors["camera"], speed, time.time())
                limit = tracker.speed_limit
                if limit is not None and speed > limit:
                    commands["throttle"] = min(commands["throttle"], 0.15)
                    if speed > limit + 15.0:
                        commands["brake"] = max(commands["brake"], 0.3)
                if tracker.stop_active:
                    commands["throttle"] = 0.0
                    commands["brake"] = 1.0
```
(L'emergency reste APRÈS ce bloc : priorité absolue préservée.)

1e. Log périodique — remplacer la ligne `print(f"[Pilote] f=...` par :
```python
                sign_info = ""
                if tracker is not None:
                    lim = f"{tracker.speed_limit:.0f}" if tracker.speed_limit else "--"
                    det = tracker.last_detection or ""
                    stop = " STOP!" if tracker.stop_active else ""
                    sign_info = f"  limite={lim}{stop} {det}"
                print(
                    f"[Pilote] f={frame_id:06d}  v={speed:5.1f} km/h  "
                    f"lidar_front={lidar[2]:5.0f}px  steer={commands['steering']:+6.1f}  "
                    f"thr={commands['throttle']:.2f}  brk={commands['brake']:.2f}  "
                    f"FPS={fps:4.1f}{sign_info}"
                )
```

- [ ] **Step 2 : Smoke test manuel (2 terminaux)**

Terminal 1 : `.venv/Scripts/python.exe -m simulator.main --server --circuit gen_000`
Terminal 2 : `.venv/Scripts/python.exe -m pilot.main --speed-target 80`
Expected : la voiture roule ; passages devant les panneaux → log `limite=30/50/90` mis à jour, ralentissement visible ; si un STOP est sur gen_000 → arrêt ~2 s puis reprise. Vérifier aussi `--no-signs` (comportement d'avant).

- [ ] **Step 3 : Commit**

```bash
git add pilot/main.py
git commit -m "feat(signs): decision dans la boucle pilote (governor limite + arret STOP, --no-signs)"
```

---

### Task 9 : `scripts/test_signs.py` — éval boucle fermée

**Files:**
- Create: `scripts/test_signs.py`

**Interfaces:**
- Consumes: tout le pipeline (Tasks 1-8). Harness in-process pattern `test_cnn.py` (pas de ZMQ).
- Produces: verdicts PASS/FAIL imprimés, exit code 0 si tout PASS.

- [ ] **Step 1 : Écrire le harness**

```python
"""
test_signs.py -- Valide la lecture de panneaux en boucle fermee (headless).

Scenarios (in-process, pattern test_cnn.py -- verite terrain issue du
placement, JAMAIS montree au pilote qui ne voit que la camera) :

  1. LIMITE : panneau 30 place sur la trajectoire PID (dry-run prealable,
     meme technique que place_signs.py). Le PID roule a speed_target=80.
     PASS si la vitesse descend sous 35 km/h dans les 6 s apres le passage.
  2. STOP : panneau STOP place pareil. PASS si v < 2 km/h est atteint dans
     les 8 s apres passage, puis v > 20 km/h dans les 10 s suivantes (reprise).

Usage : python scripts/test_signs.py [--circuit gen_000]
"""

import argparse
import math
import os
import sys
from pathlib import Path

os.environ.setdefault("SDL_VIDEODRIVER", "dummy")
os.environ.setdefault("SDL_AUDIODRIVER", "dummy")

import numpy as np
import pygame

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))

from simulator.track import Track
from simulator.car import Car
from simulator import physics, sensors
from simulator.signs import RoadSign
from pilot.control import pid_policy
from pilot.signs import SignTracker

FPS = 60


def _dry_run_place(track: Track, t_place: float, kind: str) -> tuple[RoadSign, int]:
    """Trajectoire PID sans panneau ; pose `kind` perpendiculairement au
    point atteint a t_place secondes. Retourne (sign, frame_du_passage)."""
    car = Car(track.start_x, track.start_y, track.start_angle)
    dt = 1.0 / FPS
    target_frame = int(t_place * FPS)
    for f in range(target_frame + 1):
        lidar = sensors.get_lidar(track, car)
        s, t, b = pid_policy(lidar, physics.speed_kmh(car), speed_target=80.0)
        car.set_controls(s, t, b)
        physics.update(car, dt)
    perp = (-math.sin(car.angle), math.cos(car.angle))
    for off in (55.0, 70.0, 85.0):
        sx, sy = car.x + perp[0] * off, car.y + perp[1] * off
        if 20 < sx < track.width - 20 and 20 < sy < track.height - 20:
            return RoadSign(sx, sy, kind), target_frame
    raise RuntimeError("placement impossible (bord de frame)")


def _closed_loop(track: Track, sign: RoadSign, seconds: float) -> list[dict]:
    """Roule le PID avec le panneau composite + SignTracker actif.
    Retourne l'historique [{t, speed, limit, stop_active}]."""
    car = Car(track.start_x, track.start_y, track.start_angle)
    tracker = SignTracker()
    dt = 1.0 / FPS
    hist = []
    for f in range(int(seconds * FPS)):
        now = f * dt
        lidar = sensors.get_lidar(track, car)
        camera = sensors.get_camera_view_from_track(track, car, signs=[sign])
        speed = physics.speed_kmh(car)
        steering, throttle, brake = pid_policy(lidar, speed, speed_target=80.0)
        tracker.update(camera, speed, now)
        if tracker.speed_limit is not None and speed > tracker.speed_limit:
            throttle = min(throttle, 0.15)
            if speed > tracker.speed_limit + 15.0:
                brake = max(brake, 0.3)
        if tracker.stop_active:
            throttle, brake = 0.0, 1.0
        car.set_controls(steering, throttle, brake)
        physics.update(car, dt)
        hist.append({"t": now, "speed": speed,
                     "limit": tracker.speed_limit,
                     "stop": tracker.stop_active})
    return hist


def scenario_limite(circuit: str) -> bool:
    track = Track(circuit, 1280, 720)
    sign, f_pass = _dry_run_place(track, t_place=6.0, kind="30")
    hist = _closed_loop(track, sign, seconds=6.0 + 6.0 + 2.0)
    t_pass = f_pass / FPS
    window = [h for h in hist if t_pass <= h["t"] <= t_pass + 6.0]
    ok_detect = any(h["limit"] == 30.0 for h in hist)
    ok_slow = any(h["speed"] <= 35.0 for h in window)
    print(f"  [limite30] detecte={ok_detect}  v<=35 dans fenetre={ok_slow}  "
          f"-> {'PASS' if ok_detect and ok_slow else 'FAIL'}")
    return ok_detect and ok_slow


def scenario_stop(circuit: str) -> bool:
    track = Track(circuit, 1280, 720)
    sign, f_pass = _dry_run_place(track, t_place=6.0, kind="stop")
    hist = _closed_loop(track, sign, seconds=6.0 + 8.0 + 10.0)
    t_pass = f_pass / FPS
    stopped = [h["t"] for h in hist if t_pass <= h["t"] <= t_pass + 8.0
               and h["speed"] < 2.0]
    ok_stop = bool(stopped)
    ok_resume = False
    if ok_stop:
        t_stop = stopped[-1]
        ok_resume = any(h["speed"] > 20.0 for h in hist
                        if t_stop < h["t"] <= t_stop + 10.0)
    print(f"  [stop] arret={ok_stop}  reprise={ok_resume}  "
          f"-> {'PASS' if ok_stop and ok_resume else 'FAIL'}")
    return ok_stop and ok_resume


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--circuit", default="gen_000")
    args = parser.parse_args()

    pygame.init()
    pygame.display.set_mode((1, 1))
    print(f"[test_signs] circuit {args.circuit}")
    results = [scenario_limite(args.circuit), scenario_stop(args.circuit)]
    pygame.quit()
    n = sum(results)
    print(f"[test_signs] {n}/2 PASS")
    sys.exit(0 if n == 2 else 1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2 : Lancer**

Run : `.venv/Scripts/python.exe scripts/test_signs.py`
Expected : `2/2 PASS`. Si FAIL : diagnostiquer avec superpowers:systematic-debugging (points usuels : panneau hors champ caméra → vérifier offset ; hystérésis jamais atteinte → baisser CONF_THRESHOLD après inspection des confiances loggées).

- [ ] **Step 3 : Commit**

```bash
git add scripts/test_signs.py
git commit -m "test(signs): scenarios boucle fermee limite-30 + stop (headless, verite terrain harness)"
```

---

### Task 10 : Non-régression conduite + doc wiki

**Files:**
- Modify: `scripts/test_cnn.py` (option `--with-signs`)
- Modify (hors repo, wiki Bureau) : `../wiki/projets/autonomous-twin/ml.md`

**Interfaces:**
- Consumes: sidecars Task 4, `load_signs` Task 2.

- [ ] **Step 1 : Ajouter `--with-signs` à `test_cnn.py`**

1a. Import (avec les autres imports simulateur) :
```python
from simulator.signs import load_signs
```

1b. Argument :
```python
    parser.add_argument("--with-signs", action="store_true",
                        help="Composite les panneaux du sidecar dans la camera (non-regression).")
```

1c. Dans `run_circuit`, signature `run_circuit(name, seconds, fps, weights, with_signs=False)` ; après la création du track :
```python
    signs = load_signs(name) if with_signs else []
```
et la ligne caméra devient :
```python
        camera = sensors.get_camera_view_from_track(track, car, signs=signs)
```
(passer `args.with_signs` à l'appel dans `main()`).

- [ ] **Step 2 : Non-régression (2 runs)**

Run : `.venv/Scripts/python.exe scripts/test_cnn.py` → Expected : **30/30 PASS** (identique à avant, chemin signs inactif).
Run : `.venv/Scripts/python.exe scripts/test_cnn.py --with-signs` → Expected : **30/30 PASS** (panneaux dans la caméra, le masque drive voit les blobs sombres des anneaux rouges). Si FAIL localisés : appliquer la contingence spec §3.6.3 (re-record dataset conduite avec panneaux + retrain) — décision à remonter à Lorenzo avant.

- [ ] **Step 3 : Commit**

```bash
git add scripts/test_cnn.py
git commit -m "test(cnn): option --with-signs pour la non-regression conduite avec panneaux"
```

- [ ] **Step 4 : Documenter dans le wiki Bureau** (`wiki/projets/autonomous-twin/ml.md`) : nouvelle décision `[D12] Feature panneaux livrée` — résumé (sprites script, placement dry-run PID, MobileNetV2 gelé features précalculées + val acc obtenue, seuils détecteur, décision governor/STOP, résultats test_signs + non-régression), date, et clore Q7 partiellement (LapTimer côté simu existe, éval ML 3-tours reste ouverte). Mettre à jour `index.md` du projet (milestone M5 ✅ si 2/2 PASS + 30/30 PASS).

- [ ] **Step 5 : Push de la branche**

```bash
git push origin dev/panneaux-lorenzo
```
(La PR vers `main` = décision Lorenzo/équipe, pas dans ce plan.)

---

## Self-review (fait à l'écriture)

- **Couverture spec** : §3.1→T1, §3.2→T2+T3+T4, §3.3→T5, §3.4→T6, §3.5→T7+T8, §3.6.1→T6 (matrice confusion) + T7 step 3, §3.6.2→T9, §3.6.3→T10. Risques §4 : mur rouge (T5 anti-mur + T7 self-check brique), troncature (T5 20% + hystérésis T7), non-régression (T10).
- **Types cohérents** : `camera` (H,W,3) uint8 partout côté pilote ; crop non transposé (W,H,3) uniquement dans `paste_into_camera` (documenté) ; `SIGN_CLASSES` défini une fois (T6), importé partout.
- **Pas de placeholder** : chaque step a son code/commande/expected complet.
